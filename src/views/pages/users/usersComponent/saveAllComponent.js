import { Button } from "@mui/material"

const save = () => {

}

export default function SaveAll() {

    return (
        <div>
            <Button variant='contained' size='large' onClick={save}>Tout sauvegarder</Button>
        </div>
    )
}